[project]
name = "production-tools-practice"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
dependencies = ["langchain>=1.3.14,<2", "pydantic>=2,<3"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

