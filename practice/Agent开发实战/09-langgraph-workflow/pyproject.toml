[project]
name = "langgraph-workflow-practice"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
dependencies = ["langgraph>=1,<2"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

